{
  "_comment": "Example overlay for RetroJet training. Merge these keys into config.json or pass a full config copy.",
  "backend": {
    "type": "retrojet",
    "retrojet": {
      "core_path": "../RetroJet/cores/snes9x2010_libretro.dll",
      "rom_path": "../RetroJet/roms/Super Mario World.sfc",
      "num_envs": 32,
      "frame_skip": 4,
      "boot_frames": 300,
      "level_ids": ["0x105", "0x106"],
      "savestate_paths": []
    }
  },
  "emulator": {
    "_comment": "When backend.type is retrojet, num_instances is used only as a fallback for backend.retrojet.num_envs."
  }
}
